"""Runtime package tests.

This directory is a package so pytest gives the module a dotted name
(`app.runtime.tests.test_runtime`) and it stops colliding with the
same-named file under `tests/` when both are collected in one run.
"""
